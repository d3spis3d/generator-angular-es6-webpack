{
  "name": "<%= appName.toLowerCase().replace(/\s/g, '-') %>",
  "version": "0.0.0",
  "dependencies": {
    "angular": "<%= ngVersion %>",
    "angular-sanitize": "<%= ngVersion %>",
    "angular-resource": "<%= ngVersion %>",
    "jquery": "^2.1.4"
  }
}
